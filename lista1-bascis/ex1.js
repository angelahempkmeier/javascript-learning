function operations(n1, n2){
    let soma = n1 + n2;
    let sub = n1 - n2;
    let mult = n1*n2;
    let div = n1/n2;
    console.log(`Soma: ${soma} \nSubtração: ${sub} \nMultiplicação: ${mult} \nDivisão: ${div}`);
}

const op1 = operations(1,1);
